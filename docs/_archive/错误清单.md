# 错误清单（旧文档断言 → 真实情况 → 证据）

> 本文档由文档重写流程（2026 年）产出：对照仓库代码、配置与构建产物逐条核实旧文档断言。
> 处置：6 份旧文档（v1/v2 优化提示词、呈现方案、理念解析、语言基调、重构方案）已移入 `docs/_archive/`，不再作为项目文档。
> 表格中的「证据」= 工具核实到的文件路径 / 命令输出摘要。

| # | 旧文档断言 | 真实情况 | 证据 | 处置 |
| --- | --- | --- | --- | --- |
| 1 | 品牌站导航为 7 项（首页/产品功能/适用场景/在线演示/文档中心/版本发布/联系），多处出现"导航骨架 7 项" | 顶部导航实际 6 项：首页/产品功能/在线演示/文档中心/版本发布/联系（无「适用场景」；`solutions` 页仍在，但从导航移除，仅首页场景区块与页脚链接进入） | `src/constants/navlinks.tsx`：6 个条目；`git log`：`9d7825a style(品牌站): 顶部导航移除「适用场景」项` | 已纠正（README/PROJECT 以 6 项为准） |
| 2 | 旧文档称"现有站为侧栏/左侧栏导航" | 实际为顶部导航（桌面横排 + 移动端抽屉），全站去侧栏 | `src/components/TopNav.tsx` 注释"顶部导航（全站去侧栏）" | 已纠正 |
| 3 | 旧文档称 `/projects/*`、`/resume` 为"影子页面需删除"，并列 6 个子页 | 这两个路由文件仍在但全部返回 `notFound()`（品牌 404），`projects/[slug]` 的 `generateStaticParams` 仅用于占位 | `src/app/projects/page.tsx`、`src/app/projects/[slug]/page.tsx`、`src/app/resume/page.tsx`：均为 `notFound()` | 已按"影子页面（下线）"描述 |
| 4 | 旧文档称演示版 `demo/` 仅含 `index.html`/`bigscreen.html` + `assets/` | `demo/` 含两个 HTML 入口（index.html 控制台、bigscreen.html 投屏窗）及 `assets/`、`cmaps/`、`standard_fonts/`、`wasm/` 四个资源目录（pdf.js、exceljs、quickjs 等产物 + 第三方许可文件） | `Get-ChildItem demo -Recurse` 实测 | 已纠正 |
| 5 | 旧文档称博客缺 `file-management` 文章、slugs 仅 9 项 | `lib/getAllBlogs.ts` 已含 `file-management`，共 10 篇；`src/app/blog/` 另有 `_template-blog-bak/` 备份目录（非文章） | `lib/getAllBlogs.ts`（10 个 slug）；`Get-ChildItem src/app/blog -Directory` | 已按 10 篇描述 |
| 6 | 旧文档称"SEO 缺失：无 og/canonical/sitemap/robots" | 已补齐：`layout.tsx` 有 openGraph/twitter metadata；每页有 canonical；`src/app/sitemap.ts` 生成 sitemap（18 个 URL）；根目录有 `robots.txt` | `src/app/layout.tsx`、`src/app/sitemap.ts`、`robots.txt`、各 `page.tsx` metadata | 已按现状描述 |
| 7 | 旧文档称"README 版本滞后 V2.2.00"、"全站数字不统一（119 vs 88）" | README 已写 V2.3.00；站点文案无 119 数字残留（`updates` 页徽章为 V2.3.00）；版本号以 `updates` 页 timeline 与 `Contact` 产品信息表为准 | `README.md`、`src/app/updates/page.tsx`、`src/components/Contact.tsx` | 已统一为 V2.3.00 |
| 8 | 旧文档称"6 大功能过时" | `src/constants/features.tsx` 已为 7 大模块（含文件管理） | `features.tsx`（7 个模块 id：console/screen/lots/archive/formula/desktop/file-management） | 已按 7 模块描述 |
| 9 | 旧文档将「拍卖系统」技术栈（React 18/Vite 5/Electron 33/sql.js）与「品牌站」技术栈混淆引用 | 本仓库（品牌站）技术栈 = Next.js 16.2.10 / React 19.2.7 / Tailwind 4.3.3 / Motion / MDX；桌面系统技术栈只出现在博客文章的"产品技术说明"语境 | `package.json`（next 16.2.10、react 19.2.7、tailwindcss 4.3.3） | 已区分两层技术栈 |
| 10 | 旧文档称品牌站技术栈含 Vite/Electron/sql.js/Vitest | 品牌站 package.json **无** vite/electron/sql.js/vitest 依赖；这些属于被展示的桌面系统（pm_Web_App），不在本仓库 | `package.json` 依赖清单 | 已纠正 |
| 11 | 旧文档大量断言「88 个自动化测试」等产品数字作为品牌站事实 | 品牌站代码本身不运行测试（无 test 脚本）；"88 用例"是博客 `test-system` 等文章描述桌面系统的内容，README 可引用为产品信息但不作为本仓库工程事实 | `package.json` scripts（dev/build/start/lint，无 test） | 措辞已限定为"产品（桌面系统）信息" |
| 12 | 旧文档描述品牌站页面信息架构时列入 `/projects`、`/resume` 作为正式页面 | 两者已下线为 404；正式路由 = `/`、`/about`、`/features`、`/solutions`、`/experience`、`/blog`、`/updates`、`/contact` | `src/app/*/page.tsx` 实测 + `sitemap.ts`（不含 projects/resume） | 已纠正 |
| 13 | 旧文档称构建命令/产物与 GitHub Pages 部署细节不清 | 已核实：`next.config.mjs` = `output:"export"`、`basePath` 由 `NEXT_PUBLIC_BASE_PATH` 控制、`trailingSlash:true`、`images.unoptimized`；`npm run build` 在 next build 后把 `demo/` 拷贝进 `out/demo/`；部署到 `https://wtw79.github.io/pm_Web_App_Showcase/` | `next.config.mjs`、`package.json` scripts、`git remote -v` | 已写入 README/PROJECT |
| 14 | 旧文档（重构方案）称"导航微调建议表"等执行蓝图 | 属执行层过时内容，且部分已按新方向落地（导航 6 项、CTA 文案已改） | 当前站点文案（如 `page.tsx` "看它工作 →"） | 归档，不进入新文档 |
| 15 | 旧文档混入理念文本（"制造身体""策展人""第零篇"）作为项目文档主体 | 品牌站理念属于站点内容定位讨论，不是仓库技术文档；保留归档供创作参考，不写入 README/PROJECT 技术正文 | 各旧文档标题与正文 | 归档 |
| 16 | `DESIGN.md` 自称"为 `pm_Web_App_Showcase/index.html` 的唯一设计依据"，描述 Marque 紫罗兰/靛蓝编辑式品牌书体系（`--violet #6D4AC4`、`--indigo #4338CA`、`--paper #FBFAFE`） | 当前站点为 Next.js App Router（`src/app/`），主题变量是**品牌青绿** `--color-brand: #0e7c86`（对齐产品主色），页面布局为顶部导航 + 卡片网格，与 DESIGN.md 的淡紫纸面/章节编号/Fig 图注体系不一致；该文档描述的是早期 index.html 静态页时期的设计方案，已过时 | `src/app/globals.css`（`--color-brand: #0e7c86`）；`src/app/layout.tsx`、`TopNav.tsx`（顶部导航）；DESIGN.md 末尾声明 | 归档 |

---

## 处置结论

- **归档**：`docs/_archive/品牌站优化提示词-v1.md`、`品牌站优化提示词-v2.md`、`品牌站呈现方案.md`、`品牌站理念解析.md`、`品牌站语言基调.md`、`品牌站重构方案.md`、`DESIGN.md`（共 7 份）。
- **重写**：`README.md`（面向访客 + 开发者）。
- **新建**：`docs/PROJECT.md`（面向维护者/接手工程师的技术说明）。
- **保留不动**：`LICENSE`（MIT + 适用范围说明，法律文件）。
